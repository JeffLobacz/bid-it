<h3><u><i><%= link_to "Bid Sheets", shifts_path %></i></u> > <b>Crew-Chief: Full Bid Sheet / Open Shifts Only </b> <%= link_to "Watchlist", shifts_cc_watched_path, class: 'btn btn-success' %></h3>
<h4><u><i><%= link_to "All Shifts", shifts_cc_path %></i></u> | <b>Open Shifts Only </b></h4>
<br>
<style>
  table {
      border-collapse: collapse;
      width: 100%;
  }

  th, td {
      text-align: center;
      padding: 3px;
  }

  tr:nth-child(even){background-color: #f2f2f2}

  th {
      background-color: #4CAF50;
      color: white;
  }
</style>
<table>
  <tr>
    <th>Watch</th>
    <th>Shift#</th>
    <th>Bidder</th>
    <th>Sunday</th>
    <th>Monday</th>
    <th>Tuesday</th>
    <th>Wednesday</th>
    <th>Thursday</th>
    <th>Friday</th>
    <th>Saturday</th>
  </tr>
  <% @shifts.each do |shift| %>
    <% if shift.employment_status == "CC" && shift.last_name == nil %>
      <tr>
        <% if shift.watched == true %>
          <td>checked</td>
        <% else %>
          <td>unchecked</td>
        <% end %>
        <td><%= shift.shift_number %></td>
        <% if shift.last_name || nil %>
          <td><%= shift.last_name %></td>
        <% else %>
          <td></td>
        <% end %>
        <td>
          <%= shift.sun_start_time.strftime("%H:%M") %>
          <%= shift.sun_quit_time.strftime("%H:%M") %>
          <%= shift.sun_location %>
        </td>
        <%# @shift = shift.shift_id %>
        <td><%#= Sunday.shift.quit_time %></td>
        <td><%#= Sunday.shift.start_time %></td>
        <td><%#= employee.seniority.strftime("%m-%d-%Y") %></td>
        <td><%#= employee.bid_time.strftime("%b %d %H:%M") %></td>
      </tr>
    <% end %>
  <% end %>
</table>
